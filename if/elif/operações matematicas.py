#treinando operações matematicas
#------------------------------------------------------------------------------------
# preço = float(input("digite o preço do produto "))
# print("[1] À vista em dinheiro ou cheque, recebe 10% de desconto ") #0,9
# print("[2] À vista no cartão de crédito, recebe 15% de desconto")#  0.85
# print("[3] Em duas vezes, preço normal de etiqueta sem juros")
# print("[4] Em duas vezes, preço normal de etiqueta mais juros de 10%") #1.1 (fazer as multiplicação)
#
# pagamento=int(input("digite a forma de pagamento "))
#
# if pagamento == 1:
#     preço_final=round( preço * 0.9)
#     print(f' preço final é {preço_final} reais')
#
# elif pagamento == 2:
#     preço_final2=round(preço * 0.85)
#     print(f'preço final é {preço_final2} reais')
#
# elif pagamento == 3:
#     preço_final3=round(preço / 2)
#     print(f'duas parcelas de {preço_final3} reais')
#
# else:
#     preço_final4=round(preço * 1.1)
#     print(f'{preço_final4} preço final do produto')

#----------------------------------------------------------------------------------

# c = float(input("temperatura em c "))
# f = (c * 1.8) + 32
# k = c + 273.15
# print(f"temperatura em c {c} \ntemperatura em f {f} \ntemperatura em kelvin {k} ")
#-----------------------------------------------------------------------------------
# kb= +int(input("digite o valor em kilobytes "))
# bits = kb * 8000
# bytes = kb * 1000
# k = kb
# megabytes = kb / 0.001
# gigabytes = kb / 0.000001
# terabytes = kb / 0.000000001
#
# print( f" em bits {bits} \n em bytes {bytes}\n em kilobytes {k}\n em megabytes {megabytes}\n em gigabytes {gigabytes} em terabytes {terabytes }")

#-------------------------------------------------------------------------------------------------

